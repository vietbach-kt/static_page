<li>
    <%= link_to user.name, user do%>
        <% if current_user.admin? && !current_user?(user) %>
            <%= link_to "Delete this user", user, method: :delete,
            data: { confirm: "You sure?" } %>
    <% end %>
</li>